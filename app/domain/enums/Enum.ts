export enum Gender {
  Male,
  Female,
}

export enum PaymentMethod {
  Cash,
  CardPayment,
}

export enum Availability {
  Available,
  NotAvailable,
}

export enum Date {
  Sunday,
  Monday,
  Tuesday,
  Wednesday,
  Thursday,
  Friday,
  Saturday,
}
